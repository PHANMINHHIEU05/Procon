package vn.ptit.procon.planner;

/** Arrival-aware contention classification for a specific Udon collection. */
public enum ArrivalContentionClassification {
    ARRIVAL_SAFE,
    ARRIVAL_TIED,
    ARRIVAL_AT_RISK,
    UNOBSERVED
}
