package vn.ptit.procon.planner;

/** Descriptive geometric relation only; it does not predict collection outcome. */
public enum ContentionClassification {
    UNOBSERVED,
    SAFE,
    TIED,
    CONTESTED
}